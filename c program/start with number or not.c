#include<stdio.h>
#include<stdio.h>
#include<math.h>
int main()
{
	int a,i;
	char str[10];
	printf("enter a number:");
	scanf("%s",str);
	if(atoi(str)) 
	{
		printf("Number start with number");
	}
	else
	{
		printf("number not start with number");
	}
}

